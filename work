Sequence Flows

Camunda 8.9
GET /v1/process-instances/{key}/sequence-flows

Camunda 8.5
Once a task is finished, the engine discards the historical data and Workaround is query Elasticsearch index.



Security Patches

8.5 is in "unmaintained" status


Scalability & Performance
- cluster performance under heavy load
- memory management
- overhead to publish data to Elasticsearch vs rdbms
- Execution Listeners - workaround to add service tasks


